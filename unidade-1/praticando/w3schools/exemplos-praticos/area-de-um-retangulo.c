#include<stdio.h>

int main() {
    int altura = 10;
    int largura = 30;
    int area;

    area = largura * altura;

    printf("A largura do retângulo é: %d\n", largura);
    printf("A área do retângulo é: %d\n", area);
    printf("A altura do retângulo é: %d\n", altura);

    return 0;
}